<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/listings.css" type="text/css" />
  <title>Listings</title>
</head>
<body>
  <%- include('partials/_header') %>
  <h1>Listings</h1>
  <form action="/listings/filter" method="post" class="price-filters">
    <input type="number" name="lowest_price" placeholder="Lowest Price">
    <input type="number" name="highest_price" placeholder="Highest Price">
    <button type="submit" value="Submit">Submit</button>
  </form>
  <section class="listings">
    <% for (let i = data.length - 1; i >= 0; i--) { %>
      <div class="listing">
        <div> <%= data[i].title %></div>
        <a href="/listings/<%= data[i].id %>">
          <% if (data[i].is_sold) { %>
            <img src="https://www.pngmart.com/files/7/Sold-PNG-Photo.png"/>
          <% } else { %>
            <img src="<%= data[i].img %>"/>
          <%  } %>
        </a>
        <div class="price">$ <%= Number(data[i].price)%></div>
      </div>
    <% } %>


</section>
</body>
</html>
